/**
 * @deprecated このファイルは後方互換のための re-export バレルです。
 * 新規コードでは `@entities/app-config/model/AppConfigContext` を直接インポートしてください。
 */
export type {
  AppConfigContextProps,
  AppConfigDerived,
} from "@entities/app-config/model/AppConfigContext";
export {
  AppConfigContext,
  FALLBACK_DERIVED,
} from "@entities/app-config/model/AppConfigContext";
