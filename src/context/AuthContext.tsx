/**
 * @deprecated このファイルは後方互換のための re-export バレルです。
 * 新規コードでは `@app/providers/auth/AuthContext` を直接インポートしてください。
 */
export type {
  AuthContextProps,
  SessionSnapshot,
} from "@app/providers/auth/AuthContext";
export { AuthContext } from "@app/providers/auth/AuthContext";
