/**
 * @deprecated このファイルは後方互換のための re-export バレルです。
 * 新規コードでは `@app/providers/theme/ThemeContext` を直接インポートしてください。
 */
export type { ThemeMode } from "@app/providers/theme/ThemeContext";
export {
  ThemeContextProvider,
  useThemeContext,
} from "@app/providers/theme/ThemeContext";
